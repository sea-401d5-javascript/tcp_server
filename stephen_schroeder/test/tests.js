'use strict';

const net = require('net');
const expect = require('chai').expect;
const createServer = require('../server');
const createClient = require('../client_server');

describe('Just starting out', () => {
  it('should post messages', (done) => {

  });
});
